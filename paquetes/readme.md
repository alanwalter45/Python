# PyPI

## Instalar paquetes
```bash
$ pip install flask
# o tambien
$ python3 -m pip install flask
```

## Desintalar paquetes
```bash
$ pip uninstall flask
# o tambien
$ python3 -m pip uninstall flask
```